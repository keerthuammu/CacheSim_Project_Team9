# FIFO Cache Replacement Policy 
## Overview
This branch contains the implementation of the **FIFO (First-In, First-Out) Cache Replacement Policy**.  
FIFO replaces the oldest cache entry first when the cache is full.

